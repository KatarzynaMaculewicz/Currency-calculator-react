import { Button, ResetButtonContainer } from "./styled";

const ResetButton = () => {
  return (
    <ResetButtonContainer>
      <Button
        type="reset"
      >
        RESET
      </Button>
    </ResetButtonContainer>
  );
};

export default ResetButton;
